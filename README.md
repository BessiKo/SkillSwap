## Платформа "Скилл Свап"

### ✨ О проекте

**«Скилл Свап»** — это PWA-платформа для обмена навыками и знаниями без прямого денежного вознаграждения. Пользователи могут размещать объявления о своих умениях (предложение) или о том, чему они хотят научиться (спрос), откликаться на них и договариваться об обмене в приватных чатах.

Проект построен на современном full-stack стеке (FastAPI + React) с использованием WebSocket для коммуникации в реальном времени и Docker для лёгкого развёртывания.

### 🛠 Стек технологий

| Компонент | Технологии | Назначение |
| :--- | :--- | :--- |
| **Backend** | Python (FastAPI), SQLAlchemy 2.0, Alembic, Redis, PyJWT | API, логика, WebSocket-сервер, кеширование сессий/SMS |
| **Database** | PostgreSQL | Основное хранилище данных, Full-Text Search |
| **Frontend** | React, TypeScript, Vite, Tailwind CSS, Zustand, React Query | PWA-приложение, UI, управление состоянием и кешированием данных |
| **Инфраструктура** | Docker, Docker Compose, GitHub Actions, Sentry | Контейнеризация, CI/CD, мониторинг ошибок |

### 🧭 Архитектура системы

Система представляет собой двухуровневое приложение:

1.  **Backend (FastAPI):** Обрабатывает REST API-запросы (CRUD, Auth) и WebSocket-соединения. Использует **PostgreSQL** как основное хранилище и **Redis** для кэширования, хранения активных WS-сессий и **Rate Limiting** (для СМС).
2.  **Frontend (React PWA):** Клиентское приложение с адаптивным интерфейсом, настроенное на использование React Query для эффективного управления состоянием сервера.

---

### 📥 Установка и запуск (Docker Compose)

Для запуска всего проекта требуется установленный **Docker** и **Docker Compose**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [адрес_вашего_репозитория]
    cd skill-swap-platform
    ```

2.  **Настройте переменные окружения:**
    Создайте файл `.env` в корне проекта на основе предоставленного примера (`.env.example`) и заполните необходимые переменные (ключи JWT, настройки БД, ключи Twilio/SMSC, и т.д.).

3.  **Запустите все сервисы:**
    ```bash
    docker compose up --build -d
    ```

4.  **Выполните миграции базы данных:**
    После того, как контейнер БД запустится, выполните миграции:
    ```bash
    docker compose exec backend alembic upgrade head
    ```

5.  **Доступ:**
    * **Backend API:** `http://localhost:8000/docs` (Swagger UI)
    * **Frontend App:** `http://localhost:3000`

---

### ✅ Реализованный функционал (MVP + Доп. фичи Спринтов 0–6)

Проект включает в себя полноценный MVP, базовую геймификацию и инструменты модерации.

#### 👤 Пользователи и Авторизация (Спринт 1)

* **Авторизация:** Регистрация/вход через **номер телефона** с подтверждением кода (SMS-Mock/Twilio).
* **Безопасность:** Реализована система JWT-токенов (`access` и `refresh`), **Middleware** для аутентификации и проверки ролей.
* **Защита:** **Rate limiting** на отправку SMS через Redis.
* **Роли:** Пользователи могут иметь роль `user` или `admin` (устанавливается вручную в БД).

#### 📜 Доска объявлений (Спринт 2)

* **CRUD-операции:** Полный набор функций для создания, просмотра, редактирования и удаления объявлений.
* **Фильтрация:** Объявления можно фильтровать по категории, требуемому уровню навыка и дате создания.
* **Поиск:** Использование **PostgreSQL Full-Text Search** для быстрого поиска по тексту объявлений.

#### 💬 Чат в реальном времени (Спринт 3)

* **Отклик:** Чат автоматически создается при первом отклике одного пользователя на объявление другого.
* **Сообщения:** Обмен сообщениями в реальном времени через **WebSocket** (`/ws/chat/{chat_id}`).
* **Архитектура:** Использование **Redis PubSub** для эффективной маршрутизации сообщений между WS-соединениями.
* **История:** Сообщения сохраняются в PostgreSQL, реализована подгрузка истории сообщений и маркировка "прочитано".

#### 🤝 Статусы сделок (Спринт 4)

Внутри чата реализован жизненный цикл сделки для контроля процесса обмена навыками:
* **Статусы:** `new` → `discussion` → `confirmed` → `completed` / `canceled`.
* **UX:** Участники могут инициировать изменение статусов (например, "Предложить обмен", "Подтвердить") с синхронизацией в реальном времени.

#### 🛡 Админ-панель (Спринт 5)

* Простая панель (на React Admin) для **модерации контента**.
* **Функции:** Просмотр списка пользователей, блокировка, скрытие/удаление объявлений.
* **Аудит:** Все действия администраторов логируются.

#### 🏅 Геймификация и Репутация (Спринт 6)

* **Репутация:** Пользователи получают **+1 к репутации** за каждую успешно завершённую сделку.
* **Отзывы:** Реализована модель `Review` для оценки и текстового отзыва после завершения сделки.
* **Достижения (Badges):** Система бейджей, которые выдаются за ключевые действия (например, "Первый обмен", "Эксперт по категории").
* **Профиль:** UI профиля с отображением уровня, прогресс-баров, бейджей и отзывов.

---

### ⏭ Что дальше (Дальнейшее развитие)

1.  **Уведомления:** Внедрение **Push-уведомлений** для новых сообщений и смены статусов.
2.  **Расширенные фильтры:** Доработка поиска и фильтров с учётом репутации и достижений.
3.  **Продвинутая аналитика:** Интеграция с аналитическими сервисами для отслеживания ключевых метрик.
